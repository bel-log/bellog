import * as React from "react";
import { createContext, useEffect, useState } from "react";

export const DocPageHome = (props) => {

    return (
        <React.Fragment>
            Doc
        </React.Fragment>

    );
}

export default DocPageHome;